name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Validate, package, and publish
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.13.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Build and validate Foundry package
        shell: pwsh
        run: ./scripts/package-release.ps1 -Tag "${{ github.ref_name }}"

      - name: Create or update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release edit "$GITHUB_REF_NAME" --prerelease
            gh release upload "$GITHUB_REF_NAME" .release/cypherv2.zip --clobber
          else
            gh release create "$GITHUB_REF_NAME" .release/cypherv2.zip \
              --title "$GITHUB_REF_NAME" \
              --generate-notes \
              --prerelease \
              --verify-tag
          fi
