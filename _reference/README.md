# Local legacy reference

`cyphersystem-3.5.2-reference.zip` is a private local backup of the project that occupied this workspace before the `cypherv2` scaffold was created.

The ZIP is intentionally excluded by `.gitignore` and must not be included in public releases.

